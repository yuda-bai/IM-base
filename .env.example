# Docker Compose 环境变量
# 复制此文件为 .env 并填入你的密码
MYSQL_ROOT_PASSWORD=YOUR_MYSQL_PASSWORD
REDIS_PASSWORD=YOUR_REDIS_PASSWORD
GINCHAT_HOST_PORT=8081
# 新环境默认使用独立数据卷；如需接入已有卷，先确认其 root 密码。
MYSQL_VOLUME_NAME=ginchat-mysql-data
