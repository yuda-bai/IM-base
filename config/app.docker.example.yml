# Docker 容器环境配置
# 复制此文件为 app.docker.yml 并填入你的真实密码
mysql:
  dns: "root:YOUR_MYSQL_PASSWORD@tcp(mysql:3306)/ginchat?charset=utf8mb4&parseTime=True&loc=Local"
redis:
  addr: "redis:6379"
  password: "YOUR_REDIS_PASSWORD"
  db: 0
  pool_size: 30
  min_idle_conns: 10
