# ==================== 开发环境 ====================
# HTTP API 基路径（Vite proxy 代理到后端，所以用 /）
VITE_API_BASE_URL=/

# WebSocket 直连后端端口（开发时 Vite 和 Go 后端不在同一端口）
VITE_WS_URL=ws://localhost:8080

# Vite 开发服务器后端代理目标
VITE_PROXY_TARGET=http://localhost:8080
